{
  "name": "alurageek",
  "version": "1.0.0",
  "description": "proyecto de Alura para FrontEnd",
  "main": "index.js",
  "scripts": {
    "dev": "json-server --watch db.json --port 3001",
    "build": "npm run build",
    "start": "npm run build && serve -s build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
